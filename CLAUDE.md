# DataGrid Framework — 작업 지침

이 프로젝트는 **스펙 로드맵을 넓혀가며 구현 → 모든 문서 갱신**을 반복하는 사이클로 진행된다.
새 세션은 이 문서만 읽으면 이전 작업을 그대로 이어갈 수 있어야 한다.

## 프로젝트 개요

순수 JavaScript / CSS 데이터그리드 프레임워크. AG Grid Design System (Community, Quartz 테마) 디자인 기반.

### 절대 원칙 (모든 작업에서 유지)

1. **의존성 0, 모듈 시스템 없음** — `<script>` 태그 로드, 전역 `DataGrid` 하나만 노출 (IIFE). `import/export`, npm 패키지, CDN 금지. ES6 문법(class, arrow 등)은 사용 가능하나 모듈 문법만 금지.
2. **테마는 CSS 토큰(`--dg-*`)으로만** — JS와 CSS에 색·크기 하드코딩 금지. 라이트/다크 모두 지원.
3. **데이터 로직은 DOM 없는 순수 함수** — `DataGrid._test`로 노출하고 Node 단위 테스트 필수.
4. **표시되는 소스 = 실행되는 소스** — 데모는 `text/x-example` 스크립트 하나로 실행과 View Source 표시를 겸한다.
5. 사용자 대면 문서·주석은 **한국어**, API 이름·코드는 영어.
6. 셀 값은 기본 HTML 이스케이프. `cellRenderer` 반환값만 예외(문서에 경고 명시됨).

## 파일 맵

| 파일 | 역할 |
|---|---|
| `dist/datagrid.js` | 프레임워크 본체 (단일 IIFE). 섹션: 헬퍼/아이콘 → **i18n(로케일 표 + `resolveLocaleText`)** → 순수 데이터 로직 → 컬럼 정규화 → **popupEditor 순수 함수** → Emitter → DataGrid 클래스(DOM 스캐폴딩 → 헤더 → 레이아웃 → 가상화 바디 → 정렬 → 필터 메뉴 → 트리 → 선택 → **에디터 위젯 팩토리(`_createEditorWidget`)** → 인라인 편집 → **팝업 폼 편집** → 클립보드 → 리사이즈/리오더 → 페이지네이션 → **무한 스크롤(`_maybeLoadMore`/상태 바)** → 오버레이 → 원격 dataSource → 데이터 API → 변경추적/undo → 상태 → CSV/Excel) → 내장 렌더러 → `_test` 노출 |
| `dist/datagrid.css` | 테마. 최상단 토큰 블록 셀렉터는 `.dg-root, .dg-menu, .dg-popup` (루트 밖에 렌더될 수 있는 팝업류는 토큰을 직접 선언 — BUG-001), 다크는 `.dg-theme-dark` + 같은 후손 셀렉터들 |
| `test/run-tests.js` | Node 단위 테스트. `node test/run-tests.js` — 실패 시 exit 1 |
| `docs/spec-roadmap.md` | **스펙 확장 기준 문서.** ParamQuery 전수 비교 기반 P1/P2/P3 체크박스 + 제안 API명. 여기서 다음 작업을 고른다 |
| `docs/api-data.js` | API 문서 데이터 (전 항목). `docs/api.html`이 렌더링. 새 API는 반드시 여기 등록 + `since` 버전 |
| `docs/api.html` | API 문서 페이지 (사이드바/검색/scrollspy — 내용 수정 불필요, 데이터만 추가하면 됨) |
| `index.html` | Template 데모 (10,000행 실전 예제, Example/View Source 카드) |
| `examples/features.html` | 기능별 데모 — 기능 추가 시 여기에 카드 추가 |
| `examples/components.html` | 디자인 시스템 갤러리 (시각 컴포넌트/상태/토큰) |
| `demo/example-tabs.js` | Example/View Source 탭 컴포넌트 |
| `demo/highlight.js` | 공용 구문 강조기 (api.html과 example-tabs가 공유) |
| `demo/data.js` | 데모 데이터 생성기 (시드 고정 PRNG — `Math.random` 사용 금지) |
| `demo/demo.css` | 데모 페이지 셸 + 예제 카드 + 코드 블록 스타일 (프레임워크 아님) |
| `demo/server.js` | 정적 서버: `node demo/server.js 8087` (`.claude/launch.json`의 `datagrid-demo`) |
| `docs/bug-reports/` | 버그 리포트 (`YYYY-MM-DD-NNN-제목.md`) |
| `docs/superpowers/specs/` | 최초 설계 스펙 (참고용) |
| `README.md` | 사용자용 요약 문서 (옵션/컬럼/API/이벤트 표) |

## 표준 작업 사이클 (스펙 1개 = 1사이클)

### 0. 상태 파악
- `git log --oneline -10`으로 최근 작업 확인
- `docs/spec-roadmap.md`에서 미체크 항목 확인. 사용자가 지정한 항목이 최우선, 없으면 P1 위에서부터.

### 1. 설계
- 로드맵의 "제안 API" 컬럼을 기본 설계로 사용. 구현 중 API가 달라지면 **로드맵 문서도 갱신**.
- 새 옵션/메서드/이벤트 이름은 기존 관례를 따른다(아래 컨벤션 참조).

### 2. 구현 (`dist/datagrid.js`)
- 정렬/필터/그룹핑/페이징 등 **계산 로직은 순수 함수**로 상단 섹션에 작성하고 `DataGrid._test`에 추가.
- 렌더링은 기존 파이프라인에 통합: `refresh()` → `_recomputeView()`(뷰 파이프라인: filter → quickFilter → sort → paginate) → `_renderHeader/_layoutColumns/_renderBody/_renderPaging/_updateOverlay`.
- 새 CSS는 `dist/datagrid.css`에 토큰 기반으로. 새 토큰을 추가하면 다크 테마 오버라이드도 함께.

### 3. 테스트 (`test/run-tests.js`)
- 새 순수 함수마다 `suite()` 추가 (엣지 케이스: null/빈 값/경계값 포함).
- `node test/run-tests.js` 전부 통과할 때까지. 기존 테스트가 깨지면 구현을 의심할 것.

### 4. 예제 페이지 반영 규칙 (스펙 구현 직후 필수)

**모든 스펙은 예외 없이 `examples/features.html`에 Example/View Source 카드를 추가한다.**
"API 전용이라 데모가 무의미하다"는 이유로 생략하지 않는다 — `getJson()` 같은 단순 getter라도
버튼 + status line으로 호출 결과를 보여주는 카드를 만든다. 추가로:

| 스펙 성격 | 추가로 반영할 곳 |
|---|---|
| 모든 스펙 (예외 없음) | `examples/features.html`에 Example/View Source 카드 **필수** — 전용 섹션(h2 + 설명 + 카드)으로 추가 |
| 실전 조합 시나리오에 어울리는 기능 (그룹핑, 상태 저장, 클립보드 등) | `index.html` 템플릿 예제에도 통합해 다른 기능과 함께 동작하는 모습을 보여준다 |
| 새 시각 요소/상태가 생긴 경우 (그룹 헤더 행, 요약 행, dirty 표시, 채우기 핸들 등) | `examples/components.html` 갤러리에 정적 견본 + 상태 변형 추가 |

**카드는 반드시 features.html의 기존 Example/View Source 형식 그대로** 작성한다.
표시되는 소스 = 실행되는 소스 원칙에 따라 `text/x-example` 스크립트 하나가 실행과 View Source 표시를 겸한다:

```html
<!-- ======================================================== 스펙 이름 -->
<h2 class="section" id="kebab-case-anchor">스펙 이름 (영문 제목)</h2>  <!-- id 필수 — api.html의 demo 링크가 참조 -->
<p class="section-desc">
  무엇을 하는 기능인지 + 핵심 API를 <code class="inline">코드</code>로 표기한 1~3문장 설명.
</p>
<div class="example-card">
  <div class="example-html">
    <!-- 조작 UI가 필요하면: <div class="toolbar"> 버튼/입력/셀렉트 </div> -->
    <div id="gridXxx" class="grid-wrap-sm"></div>  <!-- sm/md/lg 중 선택 -->
    <div class="status-line" id="xxxStatus">이벤트/호출 결과를 보여줄 안내 문구</div>
  </div>
  <script type="text/x-example">
    var grid = new DataGrid(document.getElementById('gridXxx'), {
      rowData: DemoData.makeEmployees(60, 7),  // 시드 고정 — Math.random 금지
      /* 이번 스펙의 옵션/컬럼 설정 */
    });
    // 버튼·이벤트 연결, status line 갱신 등 — 복사하면 그대로 실행되는 자체 완결형 코드
  </script>
</div>
```

- **새 섹션은 항상 features.html의 맨 마지막에 추가한다** — 마지막 기존 섹션(현재 Overlays) 뒤,
  페이지 닫는 `</div>`와 `<script src=...>` 블록 앞. 기존 섹션들 사이에 끼워 넣지 않는다
  (페이지가 구현 연대순으로 쌓이게 유지).
- 컨테이너 id(`gridXxx`)와 status id는 페이지 안에서 유일해야 한다 (기존 카드와 충돌 금지).
- `ExampleTabs.init()`이 카드를 Example/View Source 탭으로 감싸므로 카드 자체에 탭 마크업을 쓰지 않는다.
- 기존 카드에 기능을 덧붙이기보다 **스펙당 전용 카드**를 새로 만드는 것을 기본으로 한다(검색·링크 용이).
- 세부 마크업 규약이 바뀌면 `demo/example-tabs.js` 상단 주석이 최종 기준.

### 5. 문서 갱신 체크리스트 (전부 필수 — 하나라도 빠지면 미완성)
- [ ] `docs/api-data.js` — 해당 섹션에 항목 추가, `since: '1.x.0'` 명시, `demo: '<features.html 섹션 앵커 id>'`로 데모 카드 연결. 새 카테고리면 섹션 추가(구조는 파일 상단 주석 참조)
- [ ] 예제 페이지 — 위 §4 규칙대로 반영했는지 확인
- [ ] `README.md` — 옵션/컬럼/API/이벤트 표에 항목 추가
- [ ] `docs/spec-roadmap.md` — 체크박스 `[x]` 처리, API명 바뀌었으면 반영
- [ ] 릴리스 단위 작업이면 `DataGrid.version`(datagrid.js)과 `version`(api-data.js) 갱신

### 6. 브라우저 검증
- 서버: `node demo/server.js 8087` (또는 launch.json `datagrid-demo`). **file:// 직접 열기는 프리뷰 제약이 있으니 반드시 HTTP로.**
- 브라우저 도구의 `javascript_tool`로 DOM 단위 검증: 렌더링 결과, 인터랙션 시나리오, **팝업류는 computed style(background/box-shadow)까지** (BUG-001 교훈).
- 라이트/다크 모두 확인. `read_console_messages`로 콘솔 에러 0 확인.
- 주의: 그리드 `refresh()`는 헤더 DOM을 재생성하므로 검증 스크립트에서 이전 element 참조는 stale — 매번 다시 query.
- 주의: 사용자가 프리뷰 패널을 직접 조작 중일 수 있음 — 검증 스크립트는 페이지 확인 후 한 번에(원자적으로) 실행.

### 7. 커밋
- 사이클당 1커밋. 메시지: 첫 줄 영어 요약(무엇을), 본문에 이유/설계 요점.
- 마지막 줄: `Co-Authored-By: Claude Fable 5 <noreply@anthropic.com>`

## 버그 처리 절차

1. **재현 먼저** — 수정 전에 브라우저/테스트로 증상 확인, 계측 데이터 수집(computed style, 값 등).
2. 근본 원인 확정 후 수정 (증상 가리기 금지).
3. `docs/bug-reports/YYYY-MM-DD-NNN-제목.md` 작성 — 형식은 [BUG-001](docs/bug-reports/2026-07-31-001-filter-menu-broken-ui.md) 참조: 증상/재현 절차/원인 분석(계측 포함)/수정 내용/검증/재발 방지.
4. 재발 방지 항목이 일반 규칙이면 이 문서(CLAUDE.md)의 "축적된 함정"에도 추가.
5. 수정 + 리포트를 한 커밋으로.

## 코드 컨벤션

- CSS 클래스는 `dg-` 접두사. 데모 셸 클래스는 접두사 없음(demo.css).
- 이벤트 이름: 과거형 (`selectionChanged`, `columnMoved`). 취소 가능 이벤트는 `before` 접두사 + `e.cancel = true` 규약(로드맵 §2.6).
- 옵션은 생성자 options 객체, 런타임 변경은 명시적 메서드(`setXxx`). jQuery식 `option()` 시그니처 금지.
- 내부 상태/메서드는 `_` 접두사. 공개 API만 문서화.
- 행 식별은 `_rowId(row)` 경유 (WeakMap 자동 발급 또는 `getRowId`).
- 예외 처리: 소비자 콜백(cellRenderer, 이벤트 핸들러 등)의 예외는 잡아서 `console.error` + 안전한 폴백. 그리드가 죽으면 안 된다.

## 축적된 함정 (실제로 겪은 것)

1. **팝업/오버레이 토큰 상속 (BUG-001)**: `.dg-root` 밖에 렌더링될 수 있는 요소는 ① 루트 안에 append + ② CSS 토큰 블록 셀렉터에 추가 + ③ 다크 오버라이드에 후손 셀렉터 추가, 세 가지 세트로. 직접 선언된 토큰이 상속보다 우선하는 것에 특히 주의.
2. **가상 행 레이아웃**: 스크롤 중 생성되는 행 셀은 `_buildRowEl` 안에서 `_applyCellLayout`을 거쳐야 폭이 적용된다.
3. **데모 데이터 결정성**: `demo/data.js`는 시드 PRNG — 검증 스크립트가 특정 값에 의존해도 안전. `Math.random` 추가 금지.
4. Windows 환경: PowerShell 5.1 (`&&` 없음), git 커밋 메시지는 here-string(`@'...'@`) 사용. here-string 파싱이 깨지면 스크래치패드에 메시지 파일을 쓰고 `git commit -F <파일>`로 우회.
5. **버튼으로 여는 팝업의 토글 (BUG-002)**: ① 같은 버튼 재클릭 = 닫기 분기, ② 바깥클릭(mousedown) 핸들러에서 그 버튼 제외 — 두 가지를 세트로. 빠지면 mousedown이 닫고 click이 재오픈해서 토글이 침묵 실패한다. 팝업 검증은 열기·재클릭 토글·다른 트리거 전환·바깥 클릭을 실제 이벤트 시퀀스(mousedown→mouseup→click)로 할 것 — `element.click()`만으론 mousedown 단계가 재현 안 됨.
6. **가상화와 scrollWidth (BUG-003)**: 스크롤 컨테이너의 scrollWidth/scrollHeight를 가상화로 제거되는 콘텐츠(absolute 행/셀)에 의존시키지 말 것 — 전체 크기를 컨테이너 치수로 명시한다(세로 `_canvasEl.style.height`, 가로 `_canvasEl.style.minWidth = max(100%, 전체폭)`). 스크롤 중 DOM을 비웠다 재구성하는 사이 강제 레이아웃이 끼면 scrollWidth가 붕괴해 브라우저가 scrollLeft를 0으로 클램프한다(스크롤바 드래그가 제자리로 튕김). 또, 프리뷰 팬이 숨겨진 상태에선 scroll 이벤트가 발화하지 않으니 스크롤 핸들러 검증은 `dispatchEvent(new Event('scroll'))`로 수동 발화할 것. 같은 이유로 문서가 포커스를 안 가진 상태(`document.hasFocus() === false`)에선 `element.blur()`가 focusout을 발화하지 않는다 — blur 커밋 검증은 `dispatchEvent(new FocusEvent('focusout', { bubbles: true }))`로.
7. **3상태 체크박스의 클릭 의도 (BUG-004)**: 브라우저는 indeterminate 체크박스 클릭에 항상 `checked=true`를 준다. "완전 체크가 불가능한 상태"(비활성 자손 등)가 존재하면 checked를 그대로 믿는 핸들러는 해제 경로를 잃고 indeterminate에 갇힌다 — 클릭 의도는 도메인 상태에서 도출할 것(체크할 것이 안 남았으면 해제로 해석). 유도(derived) 표시는 유도 규칙이 성립하는 모드에서만 쓰고, 아니면 원본 상태 표시로 폴백. 3상태 UI 검증은 체크→해제→재체크 왕복 + 비활성 포함 케이스를 반드시 돌릴 것.
8. **에디터 내부 이벤트의 버블 (BUG-005)**: 에디터 안에서 발생해 캔버스로 버블되는 click/dblclick이 "편집을 (재)시작하는 핸들러"에 다시 잡히면 에디터 DOM이 재생성돼 네이티브 UI(select 드롭다운 등)가 열리자마자 닫힌다 — 편집 중 같은 셀 좌표면 조기 반환이 기본. 에디터 검증은 재생성이 티 나지 않는 text 말고 **select 드롭다운·range 드래그처럼 재생성이 드러나는 에디터로 클릭 상호작용까지** 확인할 것.
9. **표시 전용 폼 요소에 disabled 금지 (BUG-006)**: disabled 폼 요소는 마우스 이벤트를 아예 발화하지 않아, 요소가 차지한 영역이 부모 셀의 클릭/더블클릭 상호작용(편집 진입 등)의 사각지대가 된다 — 표시 전용은 `pointer-events: none` + `tabindex="-1"` 조합이 기본. 또한 `dispatchEvent` 합성 이벤트는 disabled 요소에서도 강제 발화·버블되므로 이 계열 버그를 재현하지 못한다 — 클릭 경로 검증은 computed `pointer-events`/`document.elementFromPoint`를 함께 볼 것.
10. **상태 전이 이벤트는 전이 완료 후 발사 (BUG-007)**: 에디터 커밋 같은 전이 중간에 이벤트를 발사하면, 리스너의 부수효과(refreshRow의 DOM 교체 → 포커스된 요소 제거 → Chrome이 blur를 동기 발화)가 전이 코드로 재진입해 이중 커밋·NotFoundError를 만든다. "닫기" 경로가 blur/focusout에 걸린 컴포넌트는 닫기를 먼저 끝내고(가드 플래그 set 포함) 이벤트를 발사할 것. 숨겨진 프리뷰 팬에선 제거성 blur가 발화하지 않으니, 재진입 검증은 이벤트 핸들러 안에서 blur를 직접 동기 발화하는 하네스로.
11. **날짜 문자열 파싱은 `parseLocalDate` 경유 (BUG-008)**: `new Date('2024-03-15')`는 명세상 **UTC 자정**으로 해석되는데 `getFullYear/getMonth/getDate`는 **로컬** 필드를 읽는다 — 입력 UTC + 출력 로컬 조합은 UTC 음수 오프셋 지역(아메리카)에서 하루가 밀린다. 시간대 표기가 없는 값은 로컬 벽시계로, `Z`/`±hh:mm`이 붙은 값과 타임스탬프는 절대 시각으로 다룰 것. 또 시간대 의존 로직의 테스트는 **시간대와 무관하게 성립하는 단언**으로 쓴다 — `getDate() === 15`는 KST에서 버그가 있어도 통과하지만 `getHours() === 0`은 파싱 기준을 직접 드러낸다. TZ 환경변수는 Windows Node에서 무시되므로("다른 시간대로 돌려보기" 불가) 이 방식이 유일한 회귀 가드다.

12. **쿼리스트링 직렬화에 `encodeURIComponent(value)` 직접 금지 (BUG-009)**: 인자에 `String()`이 강제로 걸려 객체·배열이 조용히 `[object Object]`가 된다. 값이 원시값이 아닐 수 있는 자리는 재귀 직렬화(`buildQueryString`)를 경유할 것. 또 **쿼리스트링의 중첩 표기에는 RFC 표준이 없다** — 닷(`page.selectPage=1`, Spring·ASP.NET)과 브래킷(`page[selectPage]=1`, qs·PHP·Rails)이 생태계별 관례로 갈리므로 "표준을 따랐다"가 아니라 **대상 서버에 맞추고 옵션으로 열어둘 것**(`dataSource.paramsFormat`, 기본 `'dot'`). 더 일반적으로, **소비자가 반환하는 구조의 자유도를 넓히는 API를 추가할 때는 그 값이 흘러가는 경로(직렬화·비교·저장)도 함께 넓혔는지 확인**한다 — 이 버그는 v2.5에서 `dataSource.request`로 반환 구조를 자유화하면서 직렬화는 평면 전제 그대로 둔 데서 생겼다.

13. **축별 client/server 모드는 서로 독립이 아니다 (v2.13)**: `sortMode`/`filterMode`/`pageMode`를 "세 축 독립"으로 설계했더니 `pageMode: 'server'` + `sortMode: 'client'`가 기본값으로 성립해 버렸고, 이 조합은 **에러 없이 틀린 결과**를 낸다 — 서버 페이징이면 클라이언트는 현재 한 페이지만 들고 있어서 정렬은 페이지 안에서만 되면서 헤더는 전체 정렬처럼 표시되고, 필터는 페이지를 걸러내는데 총 건수는 서버 값이라 표시가 어긋난다. 직교해 보이는 옵션이라도 **한쪽이 다른 쪽의 데이터 가시 범위를 좁히면 직교가 아니다** — 그런 축은 기본값을 상속시키고, 상속 방향은 "제약을 만드는 쪽 → 제약을 받는 쪽" 단방향으로만 둔다(역방향인 `sortMode: 'server'` + 클라 페이징은 정상 조합이라 막으면 안 된다). 어긋난 조합을 명시로 지정한 경우는 의도일 수 있으니 막지 말고 `console.warn`. 경고를 내는 정규화 함수는 **경고 문자열을 출력하지 말고 `warnings` 배열로 반환**해야 단위 테스트에서 콘솔이 오염되지 않는다.

14. **위젯을 다른 컨테이너로 재사용할 땐 원래 컨테이너의 어떤 성질에 기대고 있었는지 확인 (v2.16)**: 검색형 select는 "고르면 커밋하고 **곧 사라진다**"는 셀 전제로 만들어져서, 고른 값을 표시하는 상태가 아예 없었다(닫힌 뒤 셀이 다시 그려주니까). 같은 위젯을 계속 살아 있는 팝업 폼에 넣자 **고르면 값은 바뀌는데 화면엔 아무 표시가 없고**, 목록이 늘 펼쳐져 다른 필드를 밀어냈다. 재사용 전에 "이 위젯이 사라진다는 전제·부모가 다시 그려준다는 전제·부모 크기가 고정이라는 전제"를 각각 따져볼 것. 특히 **눈에 보이는 상태를 부모에게 위임하고 있던 위젯**은 그 부모가 없어지면 조용히 상태를 잃는다.
15. **진입 애니메이션의 `from` 상태는 그 자체로 사용 가능해야 한다 (v2.16)**: "화면 밖 → 제자리"를 `requestAnimationFrame` + 클래스 토글이나 CSS 트랜지션/`@keyframes`로 구현하면, **프레임이 생성되지 않는 환경**(숨겨진 프리뷰 팬, 백그라운드 탭, `display:none` 조상)에서 애니메이션이 `from`에 멈춰 요소가 영구히 안 보인다 — 세 방식 모두 같은 실패 모드다(rAF는 콜백이 안 돌고, 트랜지션·애니메이션은 타임라인이 안 흐른다). **가시성·배치를 애니메이션에 의존시키지 말 것**: 정지 상태가 최종 위치이고, 움직이는 거리는 멈춰도 보이는 정도(수십 px, 미세한 scale)로 둔다. 검증 측면에서도 이 환경에선 애니메이션 완료를 관찰할 수 없으니, `getBoundingClientRect` + `document.elementFromPoint`로 **"애니메이션이 안 돌아도 쓸 수 있는가"**를 단언하는 편이 낫다.
16. **데모 서버에 캐시 헤더가 없으면 고친 코드가 반영되지 않는다 (v2.16)**: `demo/server.js`·`server.py`는 이제 `Cache-Control: no-store`를 보낸다. 예전엔 헤더가 없어 브라우저 휴리스틱 캐시가 옛 `dist/datagrid.js`를 붙들었고, 수정 후 브라우저 검증이 옛 동작을 보여줘 없는 버그를 쫓게 만들었다. **브라우저 검증 결과가 코드와 안 맞으면 먼저 `DataGrid.version`이나 방금 넣은 식별자(`String(fn).includes('...')`)로 서빙된 소스가 최신인지 확인할 것.**
17. **반응형 폼은 "무엇이 먼저 양보하는가"를 정해 둬야 한다 (BUG-011)**: 고정폭 라벨(120px) + 고정 2열(`1fr 1fr`)을 같이 쓰면 좁은 폭에서 반드시 깨진다 — 520px 팝업에서 필드 227px, 라벨이 120px을 먹고 남은 95px을 필드 버튼 2개가 나눠 가져 **입력이 22px로 뭉개졌다**. 양보 순서를 명시할 것: ① 열 수(`repeat(auto-fit, minmax(...))`로 자동 강등) → ② 버튼 배치(`flex-wrap` + 입력에 `flex: 1 1 <최소폭>`) → ③ 라벨 위치(컨테이너 쿼리로 위로 스택). flex에서 `flex: none` 형제가 있으면 줄어드는 부담이 `flex: 1` 하나에 쏠리므로 최소 크기를 명시하고 넘치면 흘려보낼 것. 그리드 트랙엔 `minmax(0, ...)`을 씌워 긴 내용이 트랙을 밀지 못하게. 검증은 한 폭이 아니라 **폭 × 옵션 매트릭스**로 `getBoundingClientRect` 오버플로·최소폭을 단언한다("보기에 괜찮다"는 특정 폭에서만 성립).
18. **공개 API와 내부 재사용이 같은 메서드를 공유하면 상태 리셋을 못 한다 (v2.14)**: `reloadData()`는 "다시 조회"이니 1페이지로 되돌리는 게 맞는데, 페이지 이동(`setPage`)·정렬·필터의 자동 재조회가 **같은 메서드를 재사용**하고 있어서 안에 리셋을 넣으면 페이지 이동이 깨진다 — 그래서 아무도 리셋하지 않는 쪽이 기본이 돼 버렸다. 공개 메서드가 "호출자의 의도"에 따라 상태를 정리해야 하는데 내부에서도 그 메서드를 부르고 있다면, **내부용 무정책 경로(`_fetchData()`)와 공개용 정책 경로(`reloadData()`)를 갈라라.** 정책은 공개 쪽에만 두고 내부 호출자는 각자 상태를 관리한다. 리셋은 요청 조립 전에 해야 파라미터에도 반영된다.
19. **"이 요소가 무엇인지" 알리는 성질은 기능 옵션의 부수효과로 두지 말 것 (BUG-012)**: 셀 텍스트 선택을 막는 `preventDefault()`가 `if (this._cellSelection)` 블록 안에만 있어서, 그 옵션을 안 켜면 셀이 일반 문단처럼 동작했다 — 클릭이 collapsed 선택을 만들고 브라우저가 그 자리에 **캐럿을 그린다**(루트가 `tabindex="0"`이라 네이티브로 포커스를 받는다). 옵션 하나로 컴포넌트의 정체성이 바뀌면 안 되므로 `user-select: none`은 `.dg-root` 레벨 정책으로 둔다. 단 **루트 안에 append되는 팝업류(`.dg-menu`/`.dg-popup`)가 이 억제를 조용히 상속**하므로 `input`·`textarea`에 `user-select: text`로 되살리는 규칙을 세트로 넣을 것(BUG-001의 이면 — 토큰이 상속되는 것과 같은 경로다). 커서 모양도 같은 범주의 약속이다: `cursor: text`는 "클릭하면 캐럿이 선다"는 뜻이라 단일 클릭 편집이 없으면 쓰면 안 된다.
20. **`position`을 오버라이드할 땐 `z-index`도 함께 되돌릴 것 (BUG-013)**: `absolute → relative`는 "떠 있던 걸 흐름으로 되돌린다"는 뜻인데 원래 규칙의 숫자 `z-index`가 남으면 그 요소가 **stacking context로 승격**되어 **자손의 z-index를 가둔다.** 셀 앵커 패널(`z-index: 30`)을 팝업 폼용 콤보박스로 바꾸며 `position`만 `relative`로 돌렸더니, 필드마다 30짜리 컨텍스트가 생겨 열린 목록의 `z-index: 31`이 형제 필드를 못 넘었고, 같은 30끼리는 DOM 순서로 결판나 **뒤에 오는 필드가 앞 필드의 드롭다운을 덮었다**(가려진 옵션은 클릭도 안 된다). 같은 파일의 multiselect/radio가 멀쩡했던 건 `absolute → static`이라 z-index가 자동 무효였기 때문 — **`static`은 이 함정이 없고 `relative`는 있다.** 일반화: **자식의 z-index가 부모보다 커도 부모의 형제를 이기지 않는다**(비교는 같은 컨텍스트 안에서만 성립). "더 큰 z-index를 줬는데 왜 가려지지?"는 거의 항상 조상이 컨텍스트를 만들고 있다는 신호다. 검증은 rect 겹침이 아니라 **`elementFromPoint`/`elementsFromPoint`로 실제 페인트 순서**를 단언하고, 프로브는 **2D 교차**로 짤 것(세로 겹침만 보면 2열 레이아웃에서 가로로 안 겹치는 필드를 범인으로 오탐한다).

20. **새 검증 규칙은 에디터 타입별 "빈 값 정규화"를 먼저 확인할 것 (v2.19)**: `column.required`를 넣고 인라인 차단을 검증하니 숫자 컬럼에서 안 걸렸다 — `number`/`date` 에디터는 빈 입력을 `newValue = value`로 **되돌리므로**(기존 정규화) `changed`가 false가 되어 검증 자체를 타지 않는다. 데이터는 안전하지만 "어디서나 막는다"는 서술은 과장이 되므로 문서와 데모를 타입에 맞춰 갈랐다. 더 일반적으로, **커밋 파이프라인에 규칙을 추가할 때는 그 앞단의 값 변환을 먼저 읽을 것** — 규칙이 도달하지 못하는 경로가 있다. 또 **"거부됐다"를 검증할 때는 반드시 대조군(거부되지 않아야 하는 케이스)을 함께 돌릴 것**: `pasteTsv('')`가 0을 반환한 걸 required의 거부로 오독했는데, 실제로는 `if (!text)` 가드였고 비필수 컬럼도 0이었다. 대조군 없이는 "막혔다"와 "애초에 실행되지 않았다"가 구분되지 않는다.

21. **뷰포트 기하로 자동 트리거를 걸 때는 "레이아웃이 없는 상태"를 먼저 확인할 것 (v2.22)**: 무한 스크롤의 바닥 판정(`scrollHeight - scrollTop - clientHeight <= threshold`)은 **숨겨진 그리드에서도 참**이다 — `display:none`이나 안 열린 탭에서는 셋 다 0이라 "바닥에 닿았다"와 구별되지 않고, 보이지도 않는 그리드가 마지막 페이지까지 전부 받아버린다. 그렇다고 `scrollHeight > clientHeight`(스크롤이 있을 것)를 요구하면 **첫 페이지가 뷰포트를 못 채우는 정상 케이스**가 죽는다 — 그때는 scroll 이벤트가 영영 안 와서 "더 있는데 멈춘 그리드"가 된다. 두 상황을 가르는 유일한 신호는 `clientHeight > 0`이다. 자동 조회는 append 직후에도 같은 판정을 다시 돌려 뷰포트가 찰 때까지 이어 받게 한다. 곁들여: **누적형 로딩은 실패 시 페이지 커서를 되돌릴 것** — 안 그러면 재시도가 실패한 페이지를 건너뛰어 데이터에 조용히 구멍이 난다(검증은 `window.fetch`를 잠깐 가로채 특정 요청만 실패시키고 다음 요청 URL의 페이지 번호를 확인).

22. **증분 연산은 기준점이 있는지 확인할 것 (BUG-013)**: `_fetchData({append:true})`는 `_currentPage += 1`로 시작한다 — "지금 페이지는 받았으니 다음 것"이라는 전제인데, 아무것도 안 받은 상태에서 불리면 0페이지를 건너뛴 채 1페이지부터 쌓여 **에러 없이 데이터에 구멍**이 생긴다. 자동 트리거(`_maybeLoadMore`)는 "진행 중인가"(`_loading`)와 "더 있는가"(`_hasMore`)만 봤고 **"시작은 했는가"**를 묻지 않았다. 전제가 "생성자가 항상 첫 조회를 한다"에 기대고 있었는데 `autoLoad: false`가 그걸 깨뜨렸다 — 다만 **첫 조회 실패 경로로는 원래부터 도달 가능**했다. 상태 판정에 행 수(`_rows.length`)를 쓰지 말 것: 씨앗 `rowData`가 있으면 행은 있는데 서버 0페이지는 없다. "무엇이 있는가"가 아니라 "무엇을 했는가"가 올바른 신호다. 검증 측면: **자동 트리거는 정상 흐름뿐 아니라 "아직 시작 안 함 / 실패 후" 상태에서도 돌릴 것**, 그리고 요청 검증에서 **건수만 세지 말고 페이지 번호를 볼 것**(0·1과 1·2는 둘 다 "2건"이다).

23. **분기 조건을 넓힐 땐 "그 위 분기가 먼저 잡는지"를 먼저 볼 것 (v2.25)**: `editorSearch`를 multiselect로 확장하며 아래쪽 분기 조건을 `(select || multiselect) && col.editorSearch`로 넓혔는데, **위에 있던 `editorType === 'multiselect'` 분기가 먼저 잡아** 새 코드에 영영 도달하지 못했다. 증상이 "옵션이 에러도 경고도 없이 무시됨"으로 **확장 전과 완전히 동일**해서 브라우저 검증 전까지 티가 안 났다 — if/else 체인에 조건을 추가할 때는 새 분기가 아니라 **선행 분기에 배제 조건(`&& !col.editorSearch`)을 넣는 것이 세트**다. 검증 측면: "새 코드가 실행되긴 했는가"를 먼저 단언할 것(`String(fn).includes('새 클래스명')`이 아니라 **DOM에 새 클래스가 실제로 붙었는지**). 또 같은 URL로 `navigate`하면 프래그먼트만 바뀌어 **문서가 재로드되지 않는다** — 검증 전에 `location.reload(true)`로 확실히 갱신하고, 소스 최신 여부는 방금 넣은 식별자로 확인한다(§16의 캐시 함정과 증상이 같다).

24. **스크롤 컨테이너 안의 absolute 드롭다운은 컨테이너 밖으로 못 나간다 (v2.25)**: `.dg-popup-body`는 `overflow-y: auto`인데, `overflow`는 한 축만 `visible`일 수 없어 **양축이 클립**된다 — 폼 아래쪽 필드의 드롭다운이 통째로 안 보이고 푸터에 덮였다. z-index 문제로 오인하기 쉽지만(BUG-013과 증상이 비슷하다) **클리핑은 z-index로 못 이긴다**. 대응 순서: ① 가장 가까운 클립 조상(`clippingRect`)을 찾고 ② 아래가 모자라면 위로 뒤집고 ③ 어느 쪽으로도 안 담기면 남은 공간에 맞춰 `max-height`를 줄여 목록 안에서 스크롤한다(잘려서 안 보이는 것보다 짧아도 보이는 편이 낫다). 검증은 rect 비교만으로 부족하다 — **`elementsFromPoint`로 목록의 위·아래 가장자리 둘 다** 실제로 목록이 잡히는지 본다(한쪽만 보면 부분 클리핑을 놓친다).

25. **"활성/커서 상태"로 키를 해석할 땐 그 상태가 *화면에 보이는지*를 함께 물을 것 (BUG-014)**: 검색형 에디터의 Enter 핸들러가 `ssActive >= 0`(커서가 살아 있는가)만 보고 토글로 해석했더니, ① **마우스로 고른 직후의 Enter가 방금 고른 항목을 도로 해제**하고(클릭은 커서를 그 항목에 남긴다 — 단일 값 select는 클릭 즉시 닫혀서 이 상태가 아예 없었다) ② 목록이 접힌 폼에서 **보이지도 않는 항목이 토글**됐다. 안 보이는 상태로 동작하면 사용자에겐 원인 없는 변경으로 보인다(함정 §7과 같은 계열 — 브라우저/내부가 준 상태를 그대로 믿은 것). **마우스 동작은 키보드 커서를 정리해 줘야 한다.** 곁들여: **두 계층이 같은 키를 나눠 가질 땐 "누가 언제 가져가는가"를 조건 하나로 못 박을 것** — 팝업의 Enter=저장 가드가 검색형 위젯이면 무조건 건너뛰는데 위젯도 목록이 닫히면 처리하지 않아, **양쪽이 서로 상대에게 미루며 Enter가 침묵**했다(여기서는 `dg-searchselect-open` 하나로 통일). 검증 측면: **키보드 경로만 돌리지 말 것** — v2.25.0 검증의 "타이핑 → Enter"는 통과했고 버그는 **마우스로 고른 뒤 Enter**라는 혼합 경로에만 있었다. 입력 수단을 섞은 시퀀스를 반드시 포함한다.

26. **하나로 합친 칸은 그 안의 차이를 숨긴다 (v2.26)**: 3단 그룹 헤더에서 "자식 그룹이 없는 그룹은 아래 줄까지 이어 붙인다"를 런의 **첫 컬럼**만 보고 판단했더니, 자식 그룹이 있는 컬럼과 없는 컬럼이 나란한 배치에서 **아래 경계선을 그어 놓고 그 아래를 몸통으로 그리는** 모순이 나왔다 — 칸 하나는 아래 선을 일부 구간에만 그릴 수 없다. **연속 항목을 하나의 칸/런으로 합쳤다면, 그 칸의 모든 속성은 구성원 *전체*에 대해 참이어야 한다**(`colIds.every(...)`). 합치는 코드와 속성을 정하는 코드가 떨어져 있으면 첫 원소로 대표시키기 쉬우니, **속성은 런이 완성된 뒤에 계산**할 것. 이 버그는 단위 테스트가 잡지 못했다 — 오히려 **틀린 동작을 정답으로 적어두고 있었다**(내가 구현을 먼저 쓰고 테스트를 그 출력에 맞춰 썼기 때문). 브라우저에서 실제 배치를 보고서야 발견했으므로, **"어떤 값이 나오는가"가 아니라 "그 값들이 서로 모순되지 않는가"를 단언**하는 편이 낫다(여기서는 "아래 선이 있는 칸 밑에는 몸통 칸이 올 수 없다").

27. **`margin: -1px`로 겹친 경계선은 위치를 누적으로 밀어낸다 (v2.26)**: 이웃한 칸의 좌우 테두리가 2px로 겹쳐 보이는 걸 `border-left + border-right + margin-left: -1px`로 해결해 뒀는데, 이러면 각 칸이 **앞선 칸 수만큼** 왼쪽으로 밀린다. 한 줄일 땐 전부 같이 밀려 안 보이지만, **줄마다 칸 수가 다른 두 번째 줄이 생기는 순간 세로선이 어긋난다**(1px씩). 겹침 해결은 위치를 건드리지 않는 방향으로 — **한쪽(오른쪽)에만 긋고** 반대쪽은 이웃이 담당하게 하거나, 레이아웃에 영향이 없는 `box-shadow: inset`을 쓴다. 검증은 "보기에 맞나"가 아니라 **줄별 경계선 x좌표 집합을 뽑아 포함관계를 단언**할 것(`row0.every(x => row1.includes(x))`) — 1px 어긋남은 눈으로 안 잡힌다.

## 버전 정책

- 로드맵 §4 기준: v1.1 = P1 완료, v1.2 = P2 완료. 항목 단위로도 `since`는 다음 릴리스 버전으로 표기.
- 버전 올릴 때: `DataGrid.version` + `api-data.js`의 `version` + README 소개 문구 동기화.

## 빠른 명령어

```bash
node test/run-tests.js        # 단위 테스트
node demo/server.js 8087      # 데모 서버 → http://localhost:8087
python demo/server.py         # Python판 데모 서버 (index.html 브라우저 자동 오픈, --no-open 지원)
```

| 확인할 것 | 위치 |
|---|---|
| 다음 할 일 | `docs/spec-roadmap.md` 미체크 P1부터 |
| API 문서 작성법 | `docs/api-data.js` 상단 주석 |
| 데모 카드 작성법 | `demo/example-tabs.js` 상단 주석 |
| 버그 리포트 형식 | `docs/bug-reports/2026-07-31-001-*.md` |
